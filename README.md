The script calculates the load on your system and sends an automated email every hour using crontab.

To run the script:

python send_report.py

To schedule the crontab , edit the crontab using the command and save.

sudo crontab -e